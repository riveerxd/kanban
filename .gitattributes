# Ensure .env files always use LF line endings (even on Windows)
# This prevents DotNetEnv parsing issues with CRLF
*.env text eol=lf
.env.* text eol=lf

# Ensure shell scripts use LF
*.sh text eol=lf

# Let Git handle line endings for other text files automatically
* text=auto

# Windows batch files should use CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf

# Binary files
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.dll binary
*.exe binary
